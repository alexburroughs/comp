use super::command;
mod data;

pub fn run(commands : Vec<command::Command>) {

    let mem_stack : data::MemStack;
    let scope_stack : data::ScopeStack;
    let num_stack : data::NumStack;

    for com in commands {

    }
}